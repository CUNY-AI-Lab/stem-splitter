# Handoff integration plan

1. Merge workshop #7 and institutional model access #8 into an isolated branch
   from canonical main. Resolve the conflicting model paths with local narration.
2. Correct the Gateway origin and canonical model configuration, preserve safe
   error metadata, and exercise recording, refusal and stale-deck behavior.
3. Run both typechecks, unit/adapter suites, existing browser and analysis gates,
   mounted identity checks and actual current Gateway receiver acceptance.
4. Push the combined source, run the native analysis-image job, and import its
   exact artifact. Rerun all evidence and source gates without weakening them.
5. Keep source acceptance separate from the private-ingress, CI-release,
   deployed institutional-login and provider acceptance requirements documented
   in the fleet integration contract. Reconcile the isolated Cloudflare
   candidate separately, preserving its later product decisions and data.

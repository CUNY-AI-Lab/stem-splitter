# Parallel migration implementation order

1. Reconcile current main/PR #6 in an isolated worktree. Preserve original changes.
2. Add the Worker adapter, dedicated bindings, shared identity verifier, Admission,
   ownership, expiring role controls, immutable audit, and bounded paid requests.
3. Correct the subtitle, false-default Remixer, single Crate, capture compatibility
   and credit export. Keep administration out of the instructor editor.
4. Run unit, Node, analyzer and browser suites; add real-crypto/workerd access tests
   and fixture browser role/export tests. Capture actual rendered states.
5. Deploy only the closed candidate, install approved provider settings privately,
   and verify live assets/authentication failures and bounded concurrent denial.
6. Review shared Doorway integration and prefix-safe paths; then conduct a real
   CUNY browser sign-in and revocation/logout acceptance run.
7. Implement Admission-backed courses and explicit student submissions; run paid
   source/model canaries and declared memory/concurrency/retention qualification.
8. Evaluate Gateway, rehearse ownership-aware import and rollback, obtain final
   acceptance, then switch traffic while retaining Railway.

Steps 1–5 have evidence in `../../review-2026-09-06.md`. Steps 6–8 remain open.
No agent may treat these written steps as completed merely because the candidate
deploys. See `../../../MIGRATION.md` for the detailed acceptance conditions.

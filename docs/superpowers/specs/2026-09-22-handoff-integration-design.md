# Combine workshop and institutional model access

Integrate reviewed handoffs #7 and #8 on canonical main. Preserve the workshop
deck, recording ownership, saved takes after clearing the deck, asynchronous
reverse decoding and stale-arrangement protection. Preserve class-shared jobs,
annotations, cached guides, teacher accounts and immutable prompt revisions.

Every model action verifies separate same-subject app and Gateway identities.
One action makes one model request. Tools-only replies receive local narration;
remix mode can propose only solo and mute actions. The CAIL Client receives the
Gateway origin and a prefix-free model ID. Safe refusal codes, retry guidance and
canonical UUID request IDs reach the browser without provider details.

Acceptance crosses the real Node/Hono/SQLite application in Chrome, including a
native recorded take inspected with FFmpeg, and the current actual Gateway
receiver with controlled Registry, catalog and provider fixtures. Native image
evidence must come from the combined source's hosted Linux/amd64 build. Fixture
tests do not establish production login, provider output or accounting.

The separate Cloudflare candidate retains its later Crate-first product choices
and release boundary. This integration does not replace that candidate or
activate a public mount. Gateway changes, if needed, remain separate PRs.

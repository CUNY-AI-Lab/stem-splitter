# Cloudflare Listening Guide handoff

Apply Steve's CAIL Gateway contract to the isolated Cloudflare candidate at
`a4d6c18`. Preserve its later Crate-first design, removal of the Remixer
assistant, owned recordings, folders, current Admission checks, expiring local
instructor roles and revision-safe administration. Keep Remixer disabled until
its signed-in live audio acceptance is complete. Do not merge the combined
Node workshop branch wholesale or change Railway.

Doorway's private WorkerIdentity supplies the app and Gateway legs for one
person. Verify both exact audiences and the subject match. Only the separately
verified Gateway leg reaches `cail-model-api` through its private service
binding. An invalid verifier configuration is 503 before token evaluation.

The Listening Guide makes one bounded model attempt with a canonical model ID,
drains terminal stream metadata/errors through EOF, propagates cancellation,
and shows safe error codes, retry advice and support IDs. Tools-only narration
is local. Gateway owns model quotas; preserve the separate Replicate split
reservations and remove model-attempt reservations. Quota is an optional display.

Preserve the frozen root analyzer dependencies. The adapter owns its exact CAIL
package pins, transport and approved payload-free logging. The shared app
accepts that transport as a server-owned dependency; ordinary Node behavior is
retained. Verify actual Worker/Hono/client boundaries and real audio/browser
regressions. Source, deployment and signed-in provider checks remain distinct.

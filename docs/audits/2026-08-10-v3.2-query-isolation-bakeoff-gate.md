# v3.2 query-isolation bake-off gate — 2026-08-10

## Scope

This gate covers the evaluation-only SAM-Audio/AudioSep bake-off harness at
commit `725316a`. It does not enable a provider route, start a prediction,
change Railway, add an application-facing SAM-Audio adapter, or migrate any
runtime to Cloudflare Workers.

The committed boundary and still-open approvals are documented in
`docs/evaluation/2026-08-10-sam-audio-bakeoff-harness.md`.

## Clean-source verification

A detached worktree was created at exact commit `725316a`. Dependencies were
installed with the frozen lockfile using Bun 1.3.14, and the literal Phase 0
command ran from that clean checkout:

```bash
npx --yes bun@1.3.14 install --frozen-lockfile
npx --yes bun@1.3.14 run test:phase0
```

Result: pass.

- TypeScript application, Railway host, and analysis-service typechecks passed.
- Shared Node suite: 172/172 passed, including all seven query-isolation bake-off
  tests.
- Analysis service and browser/server parity suite: 22/22 passed.
- Railway SQLite/config suite: 22/22 passed.
- Local separator Python suite: 5/5 passed.
- Instrument-discovery Python suite: 30/30 passed.
- YAMNet comparator Python suite: 9/9 passed.
- Local-hosting Playwright suite: 19/19 passed.
- Authoritative server-Auto Playwright suite: 6/6 passed.
- Query-isolation shadow Playwright suite: 1/1 passed and started no provider.
- The clean worktree remained free of tracked changes after the gate.

## Offline fixture and scorer evidence

All eight ignored/private objective fixtures were regenerated before the clean
gate and then verified against their committed manifest:

```bash
node --experimental-strip-types scripts/prepare-query-isolation-bakeoff.mts
node --experimental-strip-types scripts/prepare-query-isolation-bakeoff.mts --verify
```

The verifier reported eight cases and zero provider calls. Each target contains
signal only in the declared positive span, both negative spans remain silent,
and target plus residual reconstructs the mixture at `-120 dB`.

The offline scorer was exercised with the exact target and residual references:

- SAM-Audio target SI-SDR: `120 dB`.
- SAM-Audio residual SI-SDR: `120 dB`.
- SAM-Audio reconstruction residual: `-120 dB`.
- AudioSep target SI-SDR: `120 dB`; residual and reconstruction remain `null`
  because its reviewed contract is target-only.

## Intentionally unverified

`REPLICATE_API_TOKEN` was absent, so the read-only remote schema check was
skipped. No paid prediction or authenticated provider request ran. The pinned
offline schema regression passed, but institutional SAM License approval,
checkpoint hashing, hosted-image provenance, AudioSep weight provenance, and a
reviewed run budget remain hard blockers before the comparison matrix may run.

Railway remains the integration target until the product is finished. Cloudflare
Workers migration remains deferred.

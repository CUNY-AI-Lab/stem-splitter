# v3.2 AutoSplit corpus and CI gate — 2026-08-10

This evidence binds the genre-diverse AutoSplit calibration and the source gate
to executable commit `0fbc62a` on `codex/v3.2-audio-pipeline`. It records local
acceptance only. No Railway variable, service, deployment, or paid separation
changed during this slice.

## Changes under test

- Commit `e67dd3b` adds the teacher-isolation shadow browser suite to the GitHub
  source gate. The literal local `test:phase0` command already included this
  suite; CI now enforces the same regression boundary.
- Commit `0fbc62a` requires every one of the eleven licensed, gitignored corpus
  files to carry an exact SHA-256 content pin. Both the FFmpeg-side evaluator
  and the real-browser evaluator reject missing or mismatched pins. Recorded
  Archive SHA-1 values remain an additional provenance check where available.
- The manifest test requires a normalized 64-character SHA-256 pin for every
  authorized file source.

## Exact detached-source gate

A clean detached worktree at `0fbc62a`, installed with Bun 1.3.14 and the
frozen lockfile, passed the literal `bun run test:phase0` command:

- app, Railway-host, and audio-analysis TypeScript checks;
- 165 shared worker tests;
- 22 audio-analysis contract and parity tests;
- 22 Railway configuration and migration tests;
- 5 local-separator tests;
- 30 instrument-discovery tests;
- 9 YAMNet comparator tests;
- 19 baseline browser E2E tests;
- 6 server-authoritative Auto E2E tests; and
- 1 teacher-isolation shadow E2E test.

`git show --check 0fbc62a` also passed.

## Exact real-audio evaluation

The same detached commit used a temporary link to the locally hydrated,
gitignored corpus. SHA-256 verification ran before decoding. The two evaluator
commands then passed:

- FFmpeg 8.1.2 at 22,050 Hz: 11 checked, 8 preferred routes, 3 accepted
  conservative alternatives, 0 rejected routes.
- Headless Chrome 151.0.7922.76 versus FFmpeg: 11 checked, 11 routing
  agreements, 0 rejected routes.

The corpus spans folk duet, orchestral, shoegaze, piano-and-strings, jazz,
hip-hop, bluegrass, synthwave, digital jazz, and an independent house/electro
control. These routing results do not replace the manual listening checks in
the corpus manifest or separator-output quality evaluation.

## Gates still open

1. Push the branch and open a pull request so GitHub can build and smoke the
   exact `audio-analysis` image natively on `linux/amd64`.
2. Only after that native job passes, provision the private `audio-analysis`
   service in the canonical Railway project with all app Auto flags still off.
3. Measure Railway CPU, memory, disk, child-process, timeout, and concurrency
   behavior; then run restart persistence and rollback drills.
4. Complete manual listening and paid-separation quality checks before any
   authoritative Auto rollout.

Cloudflare Workers migration remains out of scope until the product is
finished.

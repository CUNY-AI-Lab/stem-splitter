# v3.2 Railway audio-analysis pre-provision gate — 2026-08-10

This evidence record is bound to local commit `c07671c` on branch
`codex/v3.2-audio-pipeline`. It verifies the committed pre-provision guard and
the canonical production posture. It does not authorize a Railway mutation,
deployment, provider call, pull request, or release.

## Authority correction

The checkout's linked same-named Railway project is legacy infrastructure. Its
`web` and `separator` services are not the canonical deployment and must not be
used as v3.2 evidence or mutation targets.

Every accepted read used these explicit canonical identifiers:

- project: `f070742b-3375-4cba-9a86-335f39273c88`;
- production environment: `b3381640-1e2f-4765-8e15-15baec599ec2`;
- app service: `f53a2915-087c-493a-a345-7a1fa73e6588`.

The canonical project currently contains only the `stem-splitter` application
service. No `audio-analysis` service exists yet.

## Committed gate

Commit `c07671c` adds:

- `scripts/check-railway-audio-analysis.mjs`, a value-free CLI that requires
  explicit project, environment, and application-service UUIDs;
- `scripts/lib/railway-audio-analysis-gate.mjs`, which validates the existing
  safe pre-provision posture and the later deployed-but-off topology;
- `tests/railway-audio-analysis-gate.test.mts`, including failure cases for
  wrong authority, unsafe flags, deployment drift, public exposure, resource
  drift, token mismatch, and secret output;
- the executable verification order and exact initial Railway limits in
  `docs/railway-audio-analysis-provisioning.md`.

The deployed-but-off contract requires exactly one private analyzer in the app
region, no public domain or volume, one replica, the reviewed Dockerfile,
`/readyz`, a 120-second healthcheck timeout, restart-on-failure with three
retries, and an exact one-vCPU/one-GB limit override. The application flags
must remain explicitly disabled.

## Clean-checkout validation

A detached checkout at exact commit `c07671c` installed the frozen dependency
graph with Bun `1.3.14` and ran the literal `test:phase0` command. All gates
passed:

- three TypeScript typechecks;
- 177 shared worker/unit tests;
- 22 analysis-service tests;
- 22 Railway server and migration tests;
- 5 local-separator tests;
- 30 instrument-discovery tests;
- 9 YAMNet comparator tests;
- 19 flags-off browser journeys;
- 6 authoritative server-Auto browser journeys;
- 1 query-isolation shadow journey.

This clean result excludes the shared checkout's concurrent uncommitted
teacher/prompt edits.

## Canonical live readback

The same committed verifier ran with the `use-railway` caller tag and a stable
session identifier:

```sh
RAILWAY_CALLER=skill:use-railway@1.3.7 \
RAILWAY_AGENT_SESSION=codex-v32-analysis-service-20260810 \
node scripts/check-railway-audio-analysis.mjs \
  --phase pre-provision \
  --project f070742b-3375-4cba-9a86-335f39273c88 \
  --environment b3381640-1e2f-4765-8e15-15baec599ec2 \
  --app-service f53a2915-087c-493a-a345-7a1fa73e6588
```

Accepted output reported schema
`stem-splitter.railway-audio-analysis-gate.v1`, phase `pre-provision`, analysis
service `absent`, feature posture `off`, `secretsPrinted: 0`, `mutations: 0`,
and `providerCalls: 0`.

The verifier also established, without emitting values, that the canonical app
is on a terminal successful deployment with its public base URL aligned, the
YouTube importer version is an exact 64-hex pin, analyzer variables are absent,
and Auto, discovery, and query-isolation flags are absent or safely off.

## Unmet provisioning prerequisite

The repository workflow defines the native `linux/amd64` analysis-image gate,
but this branch has not been pushed and has no pull request. Therefore no native
GitHub run exists for commit `c07671c`. The provisioning runbook makes that run
a prerequisite, so creating the Railway service now would skip a reviewed
release gate and begin incurring resources without accepted native-image
evidence.

The next authorized external sequence is: publish the branch for native CI,
accept the pinned-image result, record the rollback baseline, provision the
private analyzer with Auto still off, run the deployed-off verifier, and only
then begin shadow evaluation. Railway variable edits redeploy by default, typed
service configuration is staged separately, and resource caps require explicit
readback; see Railway's [environment CLI](https://docs.railway.com/cli/environment),
[Manage Services API](https://docs.railway.com/integrations/api/manage-services),
and [cost-control documentation](https://docs.railway.com/pricing/cost-control).

No Railway service, domain, volume, variable, deployment, or provider prediction
was created or changed while producing this evidence.
